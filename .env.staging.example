# Veřejná URL tRPC API pro staging build (`vite build --mode staging`).
VITE_API_URL=

# Volitelné: absolutní prefix pro tiles. Prázdné = `/tiles` na stejném hostu jako SPA.
# VITE_TILES_URL_PREFIX=https://stage.example.com/tiles
